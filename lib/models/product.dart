import 'package:flutter/material.dart';

class Product {
  final String name, price, desc, imgPath;

  Product({
    required this.name,
    required this.price,
    required this.desc,
    required this.imgPath,
  });
}
